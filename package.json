{
  "name": "prepareprojectforllmprompt",
  "displayName": "PrepareProjectForLLMPrompt",
  "description": "Automatically prepares and formats small code projects for input into language models.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.82.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.prepareForLLM"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "properties": {
        "prepareForLLM.exclusions": {
          "type": "array",
          "default": [
            "node_modules",
            ".git"
          ],
          "description": "Folders or files to exclude."
        },
        "prepareForLLM.fileTypeFilter": {
          "type": "array",
          "default": [
            ".js",
            ".ts",
            ".py"
          ],
          "description": "File types to include."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p ./ --outDir ./out/test",
    "watch-tests": "tsc -p ./ -w --outDir ./out/test",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/sinon": "^10.0.16",
    "@types/vscode": "^1.82.0",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "sinon": "^16.0.0",
    "ts-loader": "^9.4.4",
    "typescript": "^5.1.6",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@dqbd/tiktoken": "^1.0.7"
  }
}
